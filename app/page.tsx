import Card from "@/components/Card";

const page = () => {
  return (
    <main className="flex items-row items-center justify-center min-h-screen bg-gray-300">
      <Card />
    </main>
  );
};

export default page;
